deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

test:
  override:
    # run test scripts for unit/integration/functional
    # - cd app && npm test
    - echo "test"

notify:
  webhooks:
    - url: http://23.20.38.137:9200/circleci/new_events