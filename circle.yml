deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

test:
  override:    
    - mocha > test-result.json

notify:
  webhooks:
    - url: http://23.20.38.137:9200/circleci/new_events